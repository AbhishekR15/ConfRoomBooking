# This is a sample build configuration for JavaScript.
# Check our guides at httpsconfluence.atlassian.comx14UWN for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
image node10.15.3

pipelines
  scratch-org-def: config/project-scratch-def.json
  assign-permset: false
  permset-name:
  run-apex-tests: true
  apex-test-format: tap
  delete-scratch-org: false
  show-scratch-org-url: false
  open-path: 
  import-data: false
  data-plans:
 